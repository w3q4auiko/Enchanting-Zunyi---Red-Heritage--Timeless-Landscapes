# -----------------------------------------------------------------------------
# 后端依赖清单
# 用于描述旅游信息系统服务层的核心库、数据库访问与安全组件，
# 以保证接口契约一致与运行期可复现。
# -----------------------------------------------------------------------------

# Web 框架与核心组件：提供路由、请求上下文与扩展生态。
Flask==3.0.3
Flask-Cors==5.0.0
Werkzeug==3.0.3
Pillow==12.3.0

# 数据库与持久化：支撑景区、美食、路线等核心数据访问。
PyMySQL==1.1.2
DBUtils==3.1.2

# 身份认证与安全：构建令牌体系与密码学能力。
PyJWT==2.8.0
cryptography==46.0.3

# 工具与辅助库：用于配置加载、时间处理与数据校验。
requests==2.32.4
python-dotenv==1.0.1
python-dateutil==2.9.0.post0
pytz==2025.2
jsonschema==4.23.0

# 生产服务运行时：提供 WSGI 服务与高并发协程支持。
gunicorn==21.2.0
gevent
